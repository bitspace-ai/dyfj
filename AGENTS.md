# AGENTS.md

If you're an agent picking up work in this repo: read **`README.md`**, especially **§1 (Decisions)**. That section is authoritative — non-goals, the five Layer 0 stances, the Goal 1 done-line, the Day-1 inter-agent-contracts posture, and the authority/policy rules.

If `§1` and prose elsewhere in the README contradict, `§1` wins.

If you only have 60 seconds, that's enough — `§1` is short by design.

If you want context on naming (this repo vs. its private overlay), see the umbrella naming in `README.md` §11 revision history; alternately the canonical convention is: **DYFJ** is the umbrella, **DYFJ Project** is this repo (`bitspace-ai/dyfj`), **DYFJ Workbench** is the private implementation (`bitspace/dyfj`, not in this repo).

That's it. Read the README.
